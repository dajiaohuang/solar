# Physical ephemeris contract

Solar Atlas uses immutable SPK files for Web/backend focus-body geometry. The two source profiles each contain 602 SHA-256-pinned files: the short-window `pages` source profile totals 272,921,600 bytes (260.3 MiB), and the full Web/backend source profile totals 1,167,867,904 bytes (1113.8 MiB). These source-profile totals are not deployed preview totals: the current curated Pages product selects a dependency closure of 36 files / 90,687,488 SPK bytes, as described in [preview delivery](./preview-delivery.md). Static Web files load on demand after the explorer's first-visit choice (or directly on analysis routes); native clients do not package SPK files, and the full-Web backend current-state path requests binary state tiles instead. Requesting a static body may load its file outside its interval, while calculation still uses only covered epochs. A documented existing static-Web approximation may be used outside coverage; bodies without one remain unavailable. Exact backend state requests never substitute an approximate fallback.

## Coverage

- `de440s` covers 2000-01-01 through 2051-01-01 for the planetary system and Earth/Moon centers.
- Added planetary-satellite and selected asteroid kernels cover 2020-01-01 through 2031-01-01 TDB in the full profile; the bundle includes 96 selected asteroid targets. The Eris/Haumea systems have the shorter window described below. Pages narrows large satellite files to **2026-01-01 through 2027-01-01 TDB**, preserving original records and the same target identities. Per-file manifest bounds are authoritative.
- Separately, Eris and Haumea primary centers cover 2020-01-01 through **2030-01-02 TDB**. Each file retains the published type 21 system trajectory and type 2 primary offset together. The 920136199/920136108 primary IDs must not be replaced with 20136199/20136108 system IDs. These two files are lazy, not core startup files.
- At UTC JD 2461287.5, the integration test resolves **597 selectable centers**. Two registry entries lack SPK states: Makemake and S/2009 S1. Makemake's Horizons fixture does not establish a resolved primary center; S/2009 S1 has no corroborated target number. Ninety-six asteroid snapshots are frozen Horizons type-21 responses for Pallas, Vesta, Hygiea, Psyche, Ida, Eros, Gaspra, Itokawa, Apophis, Ryugu, Phaethon, Florence, Didymos, Toutatis, Ganymed, Betulia, Steins, 1998 OR2, 1950 DA, 2001 FO32, Arrokoth, APL, Dinkinesh, 2008 EV5, Kamoʻoalewa, 2000 ET70, 2001 SN263, 2005 YU55, 2003 SD220, 2004 BL86, Duende, Hebe, Metis, Irene, Melpomene, Fortuna, Antiope, Kleopatra, Parthenope, Egeria, Lutetia, Themis, Amphitrite, Laetitia, Nysa, Astraea, Flora, Victoria, Massalia, Harmonia, Cruithne, Golevka, 1986 DA, Besixdouze, Torifune, Kalliope, Eugenia, Minerva, Hermione, Elektra, Juno, Iris, Euphrosyne, Europa, Cybele, Phocaea, Euterpe, Urania, Circe, Fides, Sylvia, Camilla, Davida, Interamnia, Thetis, Thalia, Proserpina, Bellona, Pomona, Nemausa, Chiron, Pholus, Chariklo, Varuna, Eunomia, Thisbe, Hektor, Agamemnon, Polyhymnia, Leukothea, Atalante, Leda, Daphne, Hestia, Doris and Pales; Bennu remains unavailable because Horizons explicitly reports that pre-computed SPK creation is not available for it. Daphnis uses the original historical SAT393 Type 17 record. Nine other Saturn moons use the distinct modern NAIF SAT415 delivery with its original DE437 dependencies, not the historical SSD container. These are explicit source and coverage boundaries, not absent observed objects.
- The satellite identity catalog contains 472 entries in addition to Earth's Moon, with 471 corroborated SPK IDs. Identity inclusion is not state coverage, a physical-property measurement, or formal discovery confirmation. This is not yet all known Solar System bodies.
- Four source-backed Jupiter irregular moons (Himalia 506, Elara 507, Pasiphae 508 and Sinope 509) now also have generated local seeds from their checked-in JUP347 SPKs. These are instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, not continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the original SPK remains the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Lysithea 510, Carme 511, Ananke 512 and Leda 513) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Callirrhoe 517, Themisto 518, Megaclite 519 and Taygete 520) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Chaldene 521, Harpalyke 522, Kalyke 523 and Iocaste 524) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Erinome 525, Isonoe 526, Praxidike 527 and Autonoe 528) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Thyone 529, Hermippe 530, Aitne 531 and Eurydome 532) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Euanthe 533, Euporie 534, Orthosie 535 and Sponde 536) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
 - Twelve additional source-backed Jupiter irregular moons (Kale 537, Pasithee 538, Hegemone 539, Mneme 540, Aoede 541, Thelxinoe 542, Arche 543, Kallichore 544, Helike 545, Carpo 546, Eukelade 547 and Cyllene 548) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states.
- Four additional source-backed Jupiter irregular moons (Kore 549, Herse 550, S/2010 J1 551 and S/2010 J2 552) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. The latter two retain their provisional JPL designations.
- Four additional source-backed Jupiter irregular moons (Dia 553, S/2016 J1 554, S/2003 J18 555 and S/2011 J2 556) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. Dia retains its S/2000 J11 alias; the latter three retain their provisional JPL designations without a unique NAIF registry-name claim.
- Four additional source-backed Jupiter irregular moons (Eirene 557, Philophrosyne 558, S/2017 J1 559 and Eupheme 560) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. Eirene, Philophrosyne and Eupheme retain their JPL discovery names and aliases; S/2017 J1 remains a provisional JPL designation without a unique NAIF registry-name claim.
- Four additional source-backed Jupiter irregular moons (S/2003 J19 561, Valetudo 562, S/2017 J2 563 and S/2017 J3 564) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These identities retain their provisional JPL designations without a unique NAIF registry-name claim.
- Four additional source-backed Jupiter irregular moons (Pandia 565, S/2017 J5 566, S/2017 J6 567 and S/2017 J7 568) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These identities retain their JPL discovery names and provisional designations without a unique NAIF registry-name claim.
- Four additional source-backed Jupiter irregular moons (S/2017 J8 569, S/2017 J9 570, Ersa 571 and S/2011 J1 572) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These identities retain their JPL discovery names and provisional designations without a unique NAIF registry-name claim.
- Four additional source-backed JUP347 Jupiter irregular moons (S/2003 J2 55501, S/2003 J4 55502, S/2003 J9 55503 and S/2003 J10 55504) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These extended target identities retain their provisional JPL designations without a unique NAIF registry-name claim.
- Four additional source-backed JUP347 Jupiter irregular moons (S/2003 J12 55505, S/2003 J16 55506, S/2003 J23 55507 and S/2003 J24 55508) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These extended target identities retain their provisional JPL designations without a unique NAIF registry-name claim, with the full profile covering 2020-01-01 through 2031-01-01 TDB.
- Four additional source-backed JUP347 Jupiter irregular moons (S/2011 J3 55509, S/2018 J2 55510, S/2018 J3 55511 and S/2021 J1 55512) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These extended target identities retain their provisional JPL designations without a unique NAIF registry-name claim, with the full profile covering 2020-01-01 through 2031-01-01 TDB.
- Four additional source-backed JUP347 Jupiter irregular moons (S/2021 J2 55513, S/2021 J3 55514, S/2021 J4 55515 and S/2021 J5 55516) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These extended target identities retain their provisional JPL designations without a unique NAIF registry-name claim, with the full profile covering 2020-01-01 through 2031-01-01 TDB.
- Four additional source-backed JUP347 Jupiter irregular moons (S/2021 J6 55517, S/2016 J3 55518, S/2016 J4 55519 and S/2018 J4 55520) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These extended target identities retain their provisional JPL designations without a unique NAIF registry-name claim, with the full profile covering 2020-01-01 through 2031-01-01 TDB.
- Six additional source-backed JUP347 Jupiter irregular moons (S/2022 J1 55521, S/2022 J2 55522, S/2022 J3 55523, source-comment descriptor S2025_j_1 55524, S/2017 J10 55525 and S/2017 J11 55526) use the same explicit local-seed boundary: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims; the checked-in JUP347 SPKs remain the source for covered geometric states. These target identities retain their JPL discovery/source-comment spellings without inferring a unique NAIF registry name; 55524 is source-identified even though it is absent from the discovery snapshot, with the full profile covering 2020-01-01 through 2031-01-01 TDB.
- Nine additional source-backed Saturn moons (Janus 610, Epimetheus 611, Atlas 615, Prometheus 616, Pandora 617, Pan 618, Pallene 633, Anthe 649 and Aegaeon 653) use the same explicit local-seed boundary from the modern NAIF SAT415 source pool: instantaneous parent-relative two-body osculating diagnostics at the recorded epoch, never continuous ephemerides, mean-element solutions, N-body integrations or precision claims. The checked-in SAT415 SPKs remain the source for covered geometric states; the full profile covers 2020-01-01 through 2031-01-01 TDB and Pages narrows the large SAT415 records to 2026-01-01 through 2027-01-01. Janus, Epimetheus, Atlas, Prometheus and Pandora use published satellite GM values; Pan, Pallene, Anthe and Aegaeon use the documented parent-only GM approximation because no checked-in satellite GM is available.
- The reviewed small-body satellite pack also exposes eight named primary centers (Quaoar, Orcus, Salacia, 1998 WW31, 2001 QW322, Kagara, 1999 OJ4 and 2003 UN284) as source-backed selectable identities. Their full-profile SPK center chains are exact-state-capable within the published 2020–2030 windows; they intentionally have no local fallback orbit or invented physical properties. This delivery evidence is separate from the broader source inventory and does not claim all small-body primaries are covered.
- If a kernel, center chain, or epoch is unavailable, only an existing documented approximate fallback may be used. Otherwise no position/trail is drawn; an unavailable reference suppresses the frame. No orbit is invented to fill a gap.

The files preserve original NAIF SPK type 2/3/17/21 records. Chebyshev, equinoctial and extended modified-difference records are evaluated directly: they are not refit or resampled. Each file is checked for its declared byte length and SHA-256 digest. See [type 21 validation](./spk21-validation.md) and the [satellite source workflow](../scripts/reference/SATELLITE-SURVEY.md).

## Source-specific center chains

Quaoar/Weywot, Orcus/Vanth, Salacia/Actaea, 1998 WW31/Sat1 and
2001 QW322/Sat1, Kagara/Haunu, 1999 OJ4/Sat1 and 2003 UN284/Sat1 each retain
the original publication's primary, companion and
system trajectory in one file. The full window is **2020-01-01/2030-01-01 TDB**;
Pages uses **2026-07-01/2027-01-01 TDB** to preserve existing coverage within
its capacity budget. `Sat1` is a source label qualified by the parent name,
not an invented formal satellite name. These eight primaries have no fallback
orbit or inferred radius/mass. Each published solution mixes DE440 satellite
fit and DE441/SB441-N16 heliocentric provenance; DE440 provides the Sun conversion.

The source-selection ledger accounts for all twelve frozen `tnosat_*`
publications: ten selected, the earlier Haumea v001 retained but not selected,
and Patroclus source-only. The Patroclus JPL082/DE440 file has primary/companion
offsets but no system trajectory. The separately archived Lucy solution 54/DE431
barycenter is older and is not silently composed into a claimed same-source fit.
Its raw companion label `Manoetius` is preserved in the ledger; this is not a
formal-name adjudication or delivered heliocentric state. The catalog now keeps
Patroclus and Manoetius in a separate `sourceOnlyBodies` identity list with the
JPL082 hash and missing-center reason; they are not selectable state bodies and
carry no orbit, GM, radius, position or velocity. This ledger covers the frozen
survey, not every publicly known satellite or every public source.

Dysnomia, Hiʻiaka and Namaka use the original published component offsets,
not new Keplerian seed orbits. Their full windows end at **2030-01-02 TDB**;
Pages narrows these large satellite records to 2026/2027. Each root reuses the
same publication's existing primary/system file plus the DE440 Sun conversion
dependency. `120136199 → 20136199`, `120136108/220136108 → 20136108` are
system-relative offsets; the named parent centers are `920136199/920136108`.
Parent-relative displays subtract those primary offsets inside the same pool.
The published solutions already mix satellite-fit DE440 and heliocentric
DE441/SB441-N16 provenance; the common-frame conversion is not a claim of a
uniform global fit. Source GM values, including Dysnomia's zero, are not used
as measured component masses. The primary/system coefficients are not duplicated
for each moon or independently refreshed from Horizons.

The standalone Daphnis SAT393 Type 17 source retains its original 2016 fitted
precessing ellipse. Its original comments condition scientific use on PDS placement or
Imaging Team publication. The [official PDS copy](https://naif.jpl.nasa.gov/pub/naif/pds/wgc/kernels/spk/sat393_daphnis.bsp)
matches the generic copy byte-for-byte (9,216 bytes; SHA-256
`8b21b3b68e5603006b67cb02197d789afa2e04925c3495363ac16fe180be2e08`),
establishing PDS placement. The source's Saturn-center record and embedded DE431
Sun/system-barycenter records are retained as published. The satellite release
metadata says DE435, but the actual container generator explicitly loaded DE431
for targets 3, 399, 10 and 6; we do not replace those original coefficients with
a newly fetched DE435 core. This historical mixed-source delivery is disclosed,
not represented as a new globally fitted solution or an uncertainty guarantee.

Every added root declares an ordered `solutionKernelIds` pool, with the root last. JUP347/348/349, URA182, the selected new URA184 inner-moon records, and NEP098 use DE442. SAT456/459 use DE441; SAT455/457/480, URA117 and NEP104 use DE440. SAT480's S/2009 S2 additionally uses its own published Saturn-center record. The modern NAIF SAT415 roots use their original container's DE437 Sun/system-barycenter and Saturn-center records; target 6 is the system barycenter, not Saturn center 699. Nested old source comments are not authority to assign one planetary core to every record in a merged container.

The Sun and parent center are resolved within the target's declared pool. Missing dependencies fail closed; unrelated files loaded for another body cannot alter that pool or an existing unbound solution. When an arbitrary observer is absent from the target pool, its independently resolved state is used: this cross-solution comparison is **not one globally fitted dynamical solution**.

The independent CSPICE N0067 oracle checks all targets in 444 added non-dependency roots at three epochs per target: 1,422 heliocentric/barycentric six-vector pairs. Tests pin the manifest, source file hashes and oracle source. Position agreement within 2e-6 km and velocity within 1e-9 km/s are numerical regression tolerances, not observational uncertainty or proof of accuracy at all dates.

## Coordinates and time

SPK records are geometric states relative to their declared body centers. The resolver follows those center links to a barycentric state and transforms supported J2000 equatorial records into the fixed ECLIPJ2000 frame. Returned kernel states remain km and km/s until an application boundary converts them.

Civil UTC dates are converted to seconds of TDB past J2000 using the NAIF leap-second table and periodic `TDB−TT` approximation: UTC → TAI → TT (`TT = TAI + 32.184 s`) → TDB. Numeric UTC Julian days are supported from 1972-01-01. Dates beyond the known leap-second/IERS confirmation window are labeled `future-uncertain` and hold the latest known offset; this is an explicit assumption, not a prediction.

## Observation and model boundaries

The application keeps three readouts distinct:

1. geometric state at the observation epoch;
2. reception light-time state at the emission epoch;
3. light-time plus stellar aberration direction.

No gravitational light deflection, atmospheric refraction, surface-observer correction, or covariance propagation is included. Precomputed SPK physics is not corrected again with a general-relativistic or J2 force term. Focus trajectories may use these SPK states, while the GPU catalog point cloud remains a bounded Keplerian visualization model.

Parent-relative osculating elements are instantaneous two-body diagnostics derived from a state snapshot. They are not a promise of future N-body propagation accuracy, even when the source state came from an SPK kernel.

Trajectory, event, and porkchop scans conservatively require each kernel to cover the whole requested window. A partially covered kernel is excluded for that scan, avoiding spurious extrema caused by an SPK/fallback discontinuity. Current-position markers may therefore be more accurate than a long trail. The status and export distinguish current coverage from full-window availability. Event uncertainty remains unestimated; numerical refinement is not physical prediction uncertainty.

The DE440 planetary solution and each satellite/asteroid solution have different fitted force models, observations, and errors. Their coefficients include the effects incorporated upstream (such as perturbations and, where modeled, relativistic/tidal terms). This implementation neither claims every such term for every body nor adds another GR/J2 precession term on top. It is an ephemeris reader, not a general-purpose full-physics simulator.

## Reproduce or expand a data pack

`npm run data:ephemerides` regenerates the legacy baseline, not the expanded satellite profiles. Do not use it as a one-command full-pack refresh. The [satellite workflow](../scripts/reference/SATELLITE-SURVEY.md) surveys, verifies and splits source crops, then `node scripts/integrate-satellite-pack.mjs VERIFIED_PLAN.json` integrates explicit source selections and their planetary dependencies into both profiles. The plan must name retained local evidence paths and source-selection reasons. It is an explicit developer operation, never an app startup request.

`SOLAR_EPHEMERIS_FROM` and `SOLAR_EPHEMERIS_TO` control source-supported baseline crop bounds, interpreted as TDB dates; they do not extend the added satellite plan automatically. Source bounds and per-file limits remain authoritative. Wider dates do not authorize extrapolation. Normal builds copy only the selected manifest's files, never stale extra files in `public/data/ephemerides`.

The frozen asteroid extension is reproducible with `scripts/integrate-horizons-asteroid-pack-next.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_NEXT_CACHE`. Its three targets (3200 Phaethon, 3122 Florence and 65803 Didymos) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. Psyche was intentionally not duplicated: its existing SB441 target is already covered under the alternate NAIF spelling 2000016. The cache is release evidence and is not required at application startup.

The follow-up extension is reproducible with `scripts/integrate-horizons-asteroid-pack-followup.mjs` and its retained local cache selected by `SOLAR_HORIZONS_ASTEROID_FOLLOWUP_CACHE`. Its seven targets (4179 Toutatis, 1036 Ganymed, 1580 Betulia, 2867 Steins, 52768 1998 OR2, 29075 1950 DA and 231937 2001 FO32) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. The cache is release evidence and is not required at application startup.

The third extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch3.mjs` and its retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH3_CACHE`. Its seven targets (6 Hebe, 9 Metis, 14 Irene, 18 Melpomene, 19 Fortuna, 90 Antiope and 216 Kleopatra) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. The cache is release evidence and is not required at application startup.

The fourth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch5.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH5_CACHE`. Its six targets (486958 Arrokoth, 132524 APL, 152830 Dinkinesh, 341843 2008 EV5, 469219 Kamoʻoalewa and 162421 2000 ET70) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. Sedna, Arawn, Ixion and other probed bodies remain explicit gaps when Horizons does not provide a matching pre-computed SPK. The cache is release evidence and is not required at application startup.

The fifth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch6.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH6_CACHE`. Its five targets (153591 2001 SN263, 308635 2005 YU55, 163899 2003 SD220, 357439 2004 BL86 and 367943 Duende) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. Bennu remains an explicit pre-computed-SPK gap, and bodies without a matching Horizons response remain unavailable. The cache is release evidence and is not required at application startup.

The sixth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch7.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH7_CACHE`. Its five targets (5 Astraea, 8 Flora, 12 Victoria, 20 Massalia and 40 Harmonia) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. The cache is release evidence and is not required at application startup.

The seventh extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch8.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH8_CACHE`. Its five targets (3753 Cruithne, 6489 Golevka, 6178 1986 DA, 46610 Besixdouze and 98943 Torifune) are direct NASA/JPL Horizons SPK responses, hashed at both the raw response and binary levels, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. The integration keeps original type-21 records and does not refit, extrapolate or apply an N-body model. Bennu and bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The eighth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch9.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH9_CACHE`. Its five targets (22 Kalliope, 45 Eugenia, 93 Minerva, 121 Hermione and 130 Elektra) are large binary or multiple main-belt systems with direct NASA/JPL Horizons SPK responses. Responses and binaries are hashed, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. Original type-21 records are retained; no companion orbit, refit, extrapolation or N-body model is inferred from the primary-body kernels. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The ninth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch10.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH10_CACHE`. Its five targets (3 Juno, 7 Iris, 31 Euphrosyne, 52 Europa and 65 Cybele) are direct NASA/JPL Horizons SPK responses. Responses and binaries are hashed, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. Original type-21 records are retained; no companion orbit, refit, extrapolation or N-body model is inferred from the primary-body kernels. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The tenth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch11.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH11_CACHE`. Its five targets (25 Phocaea, 27 Euterpe, 30 Urania, 34 Circe and 37 Fides) are direct NASA/JPL Horizons SPK responses. Responses and binaries are hashed, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. Original type-21 records are retained; no companion orbit, refit, extrapolation or N-body model is inferred from the primary-body kernels. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The eleventh extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch12.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH12_CACHE`. Its four targets (87 Sylvia, 107 Camilla, 511 Davida and 704 Interamnia) are direct NASA/JPL Horizons SPK responses. Responses and binaries are hashed, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. Original type-21 records are retained; no companion orbit, refit, extrapolation or N-body model is inferred from the primary-body kernels. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The twelfth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch13.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH13_CACHE`. Its five targets (17 Thetis, 23 Thalia, 26 Proserpina, 28 Bellona and 32 Pomona) are direct NASA/JPL Horizons SPK responses. Responses and binaries are hashed, then cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows. Original type-21 records are retained; no companion orbit, refit, extrapolation or N-body model is inferred from the primary-body kernels. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The thirteenth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch14.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH14_CACHE`. Its five targets (51 Nemausa, 2060 Chiron, 5145 Pholus, 10199 Chariklo and 20000 Varuna) are direct NASA/JPL Horizons SPK responses, frozen with raw-response and binary SHA-256 hashes before integration. The original type-21 heliocentric records are retained and cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows; no refit, extrapolation, companion orbit or N-body model is introduced. Chiron, Pholus, Chariklo and Varuna are outer-system small bodies commonly classified as Centaurs/TNOs, but this pack makes no taxonomy, companion-system, physical-property or shared-fit claim: each target is only the direct Horizons center state with its recorded source identity. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The fourteenth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch15.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH15_CACHE`. Its four targets (15 Eunomia, 88 Thisbe, 624 Hektor and 911 Agamemnon) are direct NASA/JPL Horizons SPK responses, frozen with raw-response and binary SHA-256 hashes before integration. The original type-21 records are retained and cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows; no refit, extrapolation, companion orbit or N-body model is introduced. Eunomia and Thisbe replace generic SB441 target spellings with direct Horizons identities. Hektor and Agamemnon are direct primary heliocentric states only: no companion or system-center state is delivered, and no binary coverage is implied. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The fifteenth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch16.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH16_CACHE`. Its four targets (33 Polyhymnia, 35 Leukothea, 36 Atalante and 38 Leda) are direct NASA/JPL Horizons SPK responses, frozen with raw-response and binary SHA-256 hashes before integration. The original type-21 records are retained and cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows; no refit, extrapolation, companion orbit or N-body model is introduced. All frozen Horizons entries are direct primary heliocentric states only: no companion or system-center state is delivered, and no binary coverage is implied. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The sixteenth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch17.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH17_CACHE`. Its four targets (41 Daphne, 46 Hestia, 48 Doris and 49 Pales) are direct NASA/JPL Horizons SPK responses, frozen with raw-response and binary SHA-256 hashes before integration. The original type-21 records are retained and cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows; no refit, extrapolation, companion orbit or N-body model is introduced. All frozen Horizons entries are direct primary heliocentric states only: no companion or system-center state is delivered, and no binary coverage is implied. Bodies without a matching Horizons response remain explicit gaps. The cache is release evidence and is not required at application startup.

The seventeenth extension is reproducible with `scripts/integrate-horizons-asteroid-pack-batch18.mjs` and the retained local cache selected by `SOLAR_HORIZONS_ASTEROID_BATCH18_CACHE`. Its four targets (2 Pallas, 4 Vesta, 10 Hygiea and 16 Psyche) replace the corresponding generic SB441 entries with direct NASA/JPL Horizons type-21 SPK responses, frozen with raw-response and binary SHA-256 hashes before integration. The original type-21 records are retained and cropped only to the same Pages 2026/2027 and full 2020/2031 TDB windows; no refit, extrapolation, companion orbit or N-body model is introduced. These are direct primary heliocentric states only: no companion or system-center state is delivered, and no binary coverage is implied. The former SB441 files are removed from the delivery manifest, so each upgraded target has one explicit source identity. The cache is release evidence and is not required at application startup.

The generator reuses published files only after byte/hash, target, source and interval validation. `SOLAR_EPHEMERIS_CACHE` selects a developer cache; `SOLAR_EPHEMERIS_REFRESH=1` explicitly bypasses reusable published and cached crops. The TNO interval is capped at its published primary endpoint even when a wider interval is requested. The Eris source assumes zero Dysnomia mass; that source-model assumption is not an observational measurement that Dysnomia is massless. Haumea uses the corrected `v001b` delivery and its own bundled JPL#110 system trajectory, not an independently refreshed Horizons solution.

## Delivery and offline behavior

The Web build and Pages preview publish their selected, validated SPK assets according to the delivery profile. Native source prototypes do not package an SPK profile: their planned first vertical slice requests exact current states through the versioned `manifest → plan → binary state tile` protocol. Native manifest and plan loading requires the user-selected HTTPS backend; a previously verified tile may be reused only when a new online plan identifies the same tile and hashes. This cache is not a complete offline ephemeris or plan/catalog restoration mechanism. Missing or invalid data stays visibly unavailable or approximate, never disguised as a physical ephemeris.

An earlier expanded Pages candidate with the pinned 1,561,171-object asteroid dataset
measured approximately 698.3 MiB (433.5 MiB catalog data, 258.5 MiB SPK assets and about
6.4 MiB application shell). That is historical candidate evidence, not the current
published preview. The live Pages build at commit `2d2b99ca17b9a287024cb661a658c5922127e9fc`
reports a curated closure of 36 SPK files totaling 90,800,128 bytes and 93.2 MiB total
capacity. Further source additions require a fresh deployment measurement; Web delivery
profiles must not silently change their validated source selection.

## Primary sources

- [NAIF SPK Required Reading](https://naif.jpl.nasa.gov/pub/naif/toolkit_docs/C/req/spk.html)
- [NAIF DAF Required Reading](https://naif.jpl.nasa.gov/pub/naif/toolkit_docs/C/req/daf.html)
- [NAIF Time Required Reading](https://naif.jpl.nasa.gov/pub/naif/toolkit_docs/C/req/time.html)
- [NAIF `naif0012.tls` leap-second kernel](https://naif.jpl.nasa.gov/pub/naif/generic_kernels/lsk/naif0012.tls)
- [JPL DE440 SPK source](https://naif.jpl.nasa.gov/pub/naif/generic_kernels/spk/planets/de440s.bsp)
